import { type ComponentPropsWithoutRef } from 'react';

export function Ul(props: ComponentPropsWithoutRef<'ul'>) {
  return (
    <ul
    
    className="my-5 list-none list-inside mx-4"
      

        {...props}>
    </ul>
  );
}
